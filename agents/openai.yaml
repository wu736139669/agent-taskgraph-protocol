interface:
  display_name: "Agent TaskGraph"
  short_description: "Coordinate native Codex or Claude Agents when parallel work pays off"
  default_prompt: "Use $agent-taskgraph to inspect this project, choose single-agent or native multi-agent execution, isolate parallel writes, and verify the integrated result."

policy:
  allow_implicit_invocation: true
