interface:
  display_name: "Agent TaskGraph"
  short_description: "Build goal-driven native Codex or Claude Agent Teams"
  default_prompt: "Use $agent-taskgraph to choose Solo, Delegation, or Team mode, then define the shared outcome, roles, task graph, collaboration protocol, lifecycle, write ownership, and integrated acceptance."

policy:
  allow_implicit_invocation: true
