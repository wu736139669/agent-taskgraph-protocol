interface:
  display_name: "Agent TaskGraph Protocol"
  short_description: "Run a PMO-led native Agent Team with isolated contexts"
  default_prompt: "Use $agent-taskgraph to understand this project, clarify the request, and create a PMO-led native Agent Team only when parallel work is useful."

policy:
  allow_implicit_invocation: true
