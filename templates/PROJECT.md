# 项目档案

> 项目级恒定信息。所有 worker 与专家开工前的"先读"文件。**由 PMO 分析项目后自动填写**（项目接入流程，见 SKILL.md 第 0 节），老板只需确认，无需手写。PMO 维护，项目级变更时更新。

## 是什么

（项目一句话定位、当前阶段）

## 技术栈与目录结构

（语言/框架/关键目录的用途——让任何新会话 30 秒理解代码在哪）

## 硬性规范（不可违反）

- （如：提交格式、日志埋点、tsc 检查、禁止内购/联网……——PR review 会检查的项）

## 共享文件锁

（多 worker 并行时的必查清单：导航注册 / package.json / 全局样式 / 路由表等——谁要改必须 PMO 批准）

## 岗位工作区映射

| 岗位 | 负责模块/目录 | 常用验收命令 |
|---|---|---|
| <岗位名，按项目需要定义> | <模块/目录> | <验收命令> |

## 专家岗配置（可选，按项目需要定义）

（本项目可能需要哪些专家岗、各自产出什么规格。例：产品架构师 → PRD；技术架构师 → 技术方案；美术指导 → 美术规范。没有就写"无"）

## 本机环境（PMO 项目接入时自动探查记录，每台机器记自己的）

| 能力 | 状态 | 说明 |
|---|---|---|
| hapi / runner | ✅/❌ | 可用则开新会话优先走 hapi |
| claude CLI 登录 | ✅/❌ | 裸 `claude -p` 是否可跑（认证状态） |
| codex 登录 | ✅/❌ | codex 会话/新线程是否可开 |
| 开新会话可用路径 | <按探查结果填：hapi runner spawn / codex 新线程 / claude CLI / 子 agent> | 供 Reviewer/worker 真实创建时选用 |
| 老板可见性偏好 | <✅ 全可见（优先 hapi 开）/ 仅在 VSCode 面板 / 无头日志即可> | 老板是否要求所有会话在 HAPI 界面可见可插话——要求则开新会话优先 hapi |
| 监控机制登记 | wait 循环：间隔 <默认 300s，可调> / 状态 <运行中 / 已退出+原因>；外部守护：<LaunchAgent/tmux/cron 标识> | **开工必须进入 wait 循环并登记；写不出 = 未开工** |

## 归档约定

- STATUS.md 只含活跃任务（done 即移除，总览板永远小）
- 已完成任务按月移入 `archive/<YYYY-MM>/`（不压缩不索引，可 grep）
- 追溯 / 批次总结翻 archive；开工只读本项目热数据，历史不加载
