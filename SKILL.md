---
name: agent-taskgraph
description: "在 Codex 或 Claude Code 中用原生多 Agent 能力拆分、并行、监督和验收复杂任务。适用于用户明确要求多 Agent、Agent Team、subagents、任务图、PMO、并行开发，或任务确有两个以上可独立推进的工作流；简单任务仍由当前会话直接完成。"
---

# Agent TaskGraph

只使用当前宿主的 **Codex** 或 **Claude Code** 原生能力。不要探测、安装或调用外部会话控制面、终端编排器或自建队列来模拟 Agent。

## 1. 先选择执行模式

先读适用的 `AGENTS.md` / `CLAUDE.md`、相关代码和验证命令，再决定是否组队。

**当前会话直接完成**：

- 单文件、小修复或同一模块内的顺序工作
- 拆分后仍会修改同一批文件
- 任务尚未清楚到可以写出独立验收标准
- Agent 协调成本不低于工作本身

**使用原生多 Agent**：

- 至少两个任务可以独立推进、独立验证
- 大范围只读探索、测试、审查或资料整理可以并行
- 写任务拥有不重叠路径，或宿主能提供独立 worktree
- 用户明确要求多 Agent、团队或并行执行

默认创建 2-3 个有当前任务的 Agent；不要为“角色完整”创建空闲 Agent。当前宿主没有原生多 Agent 能力时，直接降级为单 Agent并说明。

用户调用本 Skill 已经授权合理的内部任务拆分和原生 Agent 创建。不要为了 Agent 数量、内部任务名或会话 ID 再要求确认；只对代码无法回答的产品决策、权限扩大和外部副作用提问。用户明确要求“先看分工再开始”时才等待分工确认。

## 2. 使用当前宿主，不做跨宿主套娃

- 在 Codex 中使用 Codex 原生 subagent / agent-thread 工具。
- 在 Claude Code 中，聚焦任务默认用 subagents；只有 teammates 必须互相讨论、认领共享任务或协作决策时才使用 Agent Teams。
- 不从 Codex shell 启动 Claude 来充当 Worker，也不从 Claude shell 启动 Codex。
- 只有实际准备派发时才读取 [`references/native-runtimes.md`](references/native-runtimes.md)，按当前暴露的工具和能力执行，不凭记忆编造命令或 flag。

## 3. 主会话是协调者，不必强制空转

主会话负责理解目标、拆分、派发、处理依赖、检查证据和最终整合。它可以继续完成一个不与 Worker 重叠的实现或集成任务，以免在 Worker 运行时空等；不得重复 Worker 已拥有的范围。

只有用户明确要求纯 PMO / lead-only，或当前批次的整合风险要求主会话保持只读时，主会话才完全不写实现。

组队时用一段短预览说明即可：

```text
模式：Codex subagents / Claude subagents / Claude Agent Team
并行任务：<Agent -> 目标 -> 写入范围 -> 验收>
主会话：<协调与自己的非重叠任务>
风险：<仅列权限、冲突或外部副作用>
```

## 4. 给每个 Agent 自包含任务

派发消息必须足以让一个空白上下文开始工作，包含：

1. **目标**：一个可观察、可验收的结果。
2. **上下文**：必须读取的 3-6 个路径、符号或直接依赖 handoff。
3. **边界**：允许写入的路径，以及明确不负责的范围。
4. **依赖**：需要等待谁；无依赖时明确写 `none`。
5. **验证**：必须运行的命令或检查。
6. **交付**：结果摘要、修改路径、验证结果和残余风险。

不要把完整用户聊天、全部计划、其他 Agent 日志或无关仓库历史复制给 Worker。若 spawn 接口暴露 `fork_turns`、`fork_context` 或类似选项，选择空白或最小上下文，并在派发消息中显式提供任务事实；不要无意继承主会话全部历史。

Worker 开工前可以补充定向搜索，但不要先通读整个仓库。任务发生变化时，主会话发送增量说明，不重新粘贴旧上下文。

## 5. 写入隔离与 Git 所有权

- 并行只读任务可以共享 checkout。
- 并行写任务必须使用独立 worktree，或拥有明确且不重叠的路径。
- 两个 Agent 不得同时修改同一文件、生成同一产物或操作同一迁移状态。
- 先记录已有 dirty worktree；任何 Agent 都不得覆盖、清理或回退用户与其他 Agent 的改动。
- Worker 默认不 `commit`、`merge`、`rebase`、打 Tag、推送或发布；只有任务明确把该动作交给它时才允许。
- 主会话负责最终 diff、冲突处理、集成验证和用户要求的 Git/发布动作。

## 6. 原生监督，不复制控制面

使用宿主提供的 Agent 列表、消息、等待、恢复和停止能力。不要创建轮询日志解析器、timer 协议或第二套 session 状态机。

- 正常运行时不要频繁 ping。
- 依赖未满足的任务不要提前派发，除非它可以先完成明确的独立部分。
- 等待本批次所有相关 Agent 返回后再宣布完成。
- 结果偏离任务时，优先在原 Agent 上给一次精确修正，保留其局部上下文。
- 同一 Agent 控制失败两次或原生能力不可用时，停止继续扩张团队，改由当前会话完成或向用户说明阻塞。

## 7. 验收以产物为准

主会话必须检查实际代码、diff、测试和生成物，不以 Agent 的“完成”声明作为通过。

普通任务由 Worker 自验、主会话复核即可。只有中高风险、跨模块集成、迁移、权限、安全或用户明确要求时，才增加独立 Reviewer。Reviewer 默认只读，并依据需求、diff 和测试证据审查，不重新实现同一任务。

## 8. 持久化状态是可选项

单次会话优先使用宿主原生任务列表和消息，不要求先初始化目录。

只有以下情况才创建或维护 `.agent-taskgraph/`：

- 用户明确要求可恢复、可审计的计划
- 工作会跨多个主会话或多个工作日
- 任务依赖较多，原生任务列表不足以保存稳定 handoff

可选目录：

```text
.agent-taskgraph/
├── PROJECT.md
├── PLAN.md
├── TEAM.md
├── STATUS.md
├── DECISIONS.md
├── tasks/<id>.md
└── archive/
```

主会话独占修改 `PLAN.md`、`TEAM.md`、`STATUS.md` 和 `DECISIONS.md`。Worker 只修改自己的任务完成区和约定代码。不要把聊天全文、长日志或可从代码重新得到的信息写入这些文件。

持久化目录保存的是任务事实和 handoff，不保证恢复原生 Agent 进程、thread 或 teammate。新主会话必须先核对真实 Agent 状态；原会话不存在时，根据最后 handoff 创建新 Agent，而不是把记录过的 session/thread ID 当成仍然在线。

## 9. 模型、权限和成本

- 默认继承当前宿主的模型、推理和权限；不要硬编码可能过时的模型名。
- 只有用户明确要求，或任务质量/成本差异足以改变结果且宿主支持时，才按 Agent 覆盖模型或推理。
- 不启用 `--yolo`、bypass、无 sandbox 或其他危险模式。若 Worker 会继承异常宽泛的父会话权限，在第一次创建前一次性说明。
- 安装、联网写操作、迁移、删除、合并、Tag、推送和发布仍遵守当前会话的授权边界；多 Agent 不扩大权限。

## 10. 完成

结束前确认：

1. 所有已创建 Agent 已完成、停止或被明确标记为失败/阻塞。
2. 主会话已检查差异并运行最终集成验证。
3. 可选持久化状态与真实结果一致。
4. 向用户简要报告完成内容、验证、未完成项和风险。
